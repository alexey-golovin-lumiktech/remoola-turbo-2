'use server';

/**
 * Contracts Actions
 *
 * Contracts are read-only records representing contractor relationships.
 * They are created automatically through the backend when payment requests are made.
 * No frontend CRUD operations are needed for this feature.
 */
