{
  "name": "@remoola/db-fixtures",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "./dist/**"
  ],
  "scripts": {
    "build": "node ../../scripts/ensure-local-development.js db-fixtures:build && yarn workspace @remoola/security-utils run build && tsc -b",
    "build:test": "tsc -p tsconfig.test.json",
    "clean": "rm -rf ./node_modules ./dist ./dist-test",
    "cleanup": "node ./dist/cli.js cleanup",
    "fill": "node ./dist/cli.js fill",
    "refresh": "node ./dist/cli.js refresh --confirm",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "TERM=dumb NO_COLOR=1 yarn build && yarn build:test && node --test dist-test/*.test.js"
  },
  "dependencies": {
    "@remoola/api-types": "^0.0.0",
    "@remoola/database-2": "^0.0.0",
    "@remoola/security-utils": "^0.0.0"
  },
  "devDependencies": {
    "@remoola/eslint-config": "^0.0.0",
    "@remoola/typescript-config": "^0.0.0",
    "@types/node": "^24.3.0",
    "typescript": "5.8.2"
  }
}
