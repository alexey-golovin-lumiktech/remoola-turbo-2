#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Make local devDep binaries (turbo, eslint, prettier) directly callable.
export PATH="./node_modules/.bin:$PATH"

# Quiet by default; failing tasks still print full stderr.
export TURBO_LOG_ORDER=stream
export TURBO_OUTPUT_LOGS=errors-only
export TURBO_TELEMETRY_DISABLED=1

# Lint / e2e only the workspaces affected by the refs being pushed. For
# repo-wide tooling or config changes, the helper falls back to the conservative
# full-repo behavior.
node ./scripts/prepush-affected.mjs "$@" || exit $?
